apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: replenix-redis
spec:
  selector:
    app: replenix-redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
  type: ClusterIP
