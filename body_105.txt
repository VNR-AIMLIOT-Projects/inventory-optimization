## User Story
**As a** supply chain analyst using the Replenix dashboard,  
**I want** the application to quickly load historical training runs and heavy demand datasets,  
**So that** my workflow remains uninterrupted and smooth even as the database grows.

## Tasks
- [x] Integrate a Redis container into the Docker Compose setup.
- [x] Add `@cache` decorators to the heaviest FastAPI endpoints.
- [x] Implement automatic cache invalidation triggered by RabbitMQ worker updates to ensure data freshness.
- [x] Update system architecture diagrams to reflect the new caching layer.
